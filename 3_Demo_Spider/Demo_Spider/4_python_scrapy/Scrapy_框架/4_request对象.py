# srapy的resquest对象
# 首先scrapy的对象是由scrapy.Resquest方法创建的
# resquest中的参数
'''
    url                 请求的url
    callback=None       请求的回掉函数，默认的是parse
    method='GET'        请求的方式
    headers=None        请求的头部
    body=None           请求的正文，可以是bytes或str
    cookie=None         请求携带的cookie
    meta=None           resquest post请求数据
    encoding='utf-8'    字符集url和body字符集
    priority=0          优先级，优先级越高，越先下载
    dont_filter=False   是否对同一个url多次请求
    errback=None        错误返回
    flags=None          标签
'''






















